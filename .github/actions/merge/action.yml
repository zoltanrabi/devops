name: 'Auto Merge Pull Request'
description: 'Automatically merges the pull request.'
inputs:
  github-token:
    description: 'GitHub token'
    required: true
runs:
  using: 'node12'
  main: './.github/scripts/merge.js'