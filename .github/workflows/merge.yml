name: "Merge"

on:
  workflow_run:
    workflows: ["Deploy"]
    types:
      - completed

run-name: >-
  ${{ github.event.workflow_run.pull_request.base.ref == 'dev' && format('Validate to INT by {0} PR Name: {1}', github.event.workflow_run.pull_request.user.login, github.event.workflow_run.pull_request.title) || 
      github.event.workflow_run.pull_request.base.ref == 'test' && format('Validate to UAT by {0} PR Name: {1}', github.event.workflow_run.pull_request.user.login, github.event.workflow_run.pull_request.title) ||
      github.event.workflow_run.pull_request.base.ref == 'main' && format('Validate to PROD by {0} PR Name: {1}', github.event.workflow_run.pull_request.user.login, github.event.workflow_run.pull_request.title)
      }}

jobs:
  merge:
    runs-on: "ubuntu-latest"
    if: >
      github.event.workflow_run.event == 'pull_request_review' &&
      github.event.workflow_run.conclusion == 'success'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: "Auto Merge Pull Request"
        uses: ./.github/actions/merge
        with:
          github-token: "${{ secrets.GITHUB_TOKEN }}"